@model IEnumerable<Spitali.Models.Spital>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HospitalNames)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Department)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeNumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Department.Departments)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HospitalNames.HospitalName1)
            </td>
            <td>
                <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.SpitalId">Edit</a> |
                <a class="btn btn-success" asp-action="Details" asp-route-id="@item.SpitalId">Details</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.SpitalId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
